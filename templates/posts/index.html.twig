{% extends 'base.html.twig' %}

{#{% block title %}Hello {{ controller_name }}!{% endblock %}#}

{% block body %}
    {#<style>#}
    {#.example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }#}
    {#.example-wrapper code { background: #F5F5F5; padding: 2px 6px; }#}
    {#</style>#}

    {#<div class="example-wrapper">#}
    {#<h1>Hello {{ controller_name }}! ✅</h1>#}

    {#This friendly message is coming from:#}
    {#<ul>#}
    {#<li>Your controller at <code><a href="{{ 'src/Controller/PostsController.php'|file_link(0) }}">src/Controller/PostsController.php</a></code></li>#}
    {#<li>Your template at <code><a href="{{ 'templates/posts/index.html.twig'|file_link(0) }}">templates/posts/index.html.twig</a></code></li>#}
    {#</ul>#}
    {#</div>#}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Envoyer</button>
    {{ form_end(form) }}

    <table class="table striped">
        <thead>
            <tr>
                <th>Title</th>
                <th>Publié ?</th>
                <th>Content
                </th>
                <th>Date</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for post in posts %}
                <tr>
                    <td>{{ post.getTitle }}</td>
                    <td>{{ post.getPublished }}</td>
                    <td>{{ post.getContent }}</td>
                    <td>{{ post.getDate|date("m/d/Y") }}</td>
                    <td></td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
